#include <stdio.h>
int main()
{
  printf("\narethmatic oprations");
  int a=8;
  int b=3;
  printf("\naddion is =%d", a + b );
  printf("\nsubtraction is =%d", a - b );
  printf("\nmultiplication is =%d", a * b );
  printf("\ndivison is =%d", a / b );
  printf("\nreminder is =%d", a % b );

  return 0;
}
